package ru.ertelecom.logger;

/**
 * Типы событий логера
 */
public enum LoggerEventType {
    INFO,
    ERROR,
    JIRA_INFO,
    JIRA_ERROR,
    GIT_INFO,
    GIT_ERROR,
    DEPLOY_INFO,
    DEPLOY_ERROR
}
